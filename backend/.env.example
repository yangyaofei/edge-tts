# ============================================================================
# TTS Bundles Configuration
# ============================================================================
# 复制此文件到 config/config.env 并修改配置

# ============================================================================
# Security Configuration
# ============================================================================
# SECRET_KEY 会在首次运行时自动生成并保存
# SECRET_KEY=your_secret_key_here

# ============================================================================
# Qwen3-TTS Configuration
# ============================================================================

# 是否启用 Qwen3-TTS (true/false)
# 如果设置为 false，Qwen3-TTS 将不会初始化
QWEN_ENABLE=true

# 模型类型选择:
# - CustomVoice: 预设 9 种说话人音色 (推荐用于通用 TTS)
# - Base: 声音克隆模型 (需要参考音频)
# - VoiceDesign: 自然语言声音设计
QWEN_MODEL_TYPE=CustomVoice

# 模型大小:
# - 1.7B: 高质量模型，需要更多显存 (推荐)
# - 0.6B: 轻量级模型，适合资源受限环境
QWEN_MODEL_SIZE=1.7B

# 设备选择:
# - 留空或注释掉: 自动检测 (优先 CUDA > MPS > CPU)
# - cuda:0: 使用第一块 NVIDIA GPU
# - mps: 使用 Apple Silicon GPU (M1/M2/M3/M4)
# - cpu: 使用 CPU (速度慢)
# QWEN_DEVICE=

# 最大生成 token 数 (默认 2048，长文本可能需要增加)
QWEN_MAX_NEW_TOKENS=2048

# ============================================================================
# Hugging Face Configuration (可选)
# ============================================================================

# Hugging Face 访问 Token (用于下载私有模型)
# HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Hugging Face 镜像站点 (中国大陆用户推荐)
# HF_ENDPOINT=https://hf-mirror.com

# ============================================================================
# Tips:
# ============================================================================
# 1. 首次运行会自动下载模型 (~3.5GB for 1.7B)，请确保网络畅通
# 2. 模型会缓存到 ~/.cache/huggingface/ 目录
# 3. 如果显存不足，可以改用 0.6B 模型
# 4. Mac 用户建议使用 MPS 或 MLX 框架获得更好性能
# 5. CUDA 用户需要安装 PyTorch with CUDA 支持
